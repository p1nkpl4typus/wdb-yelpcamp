<% include ../partials/header %>

<div class="container">
    <div class="row">
        <h1 style="text-align: center;">Edit Comment</h1>
        <div style="width: 30%; margin: 30px auto;">
            <form action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
                <div class="form-group">
                     <input class="form-control" type="text" name="comment[text]" value="<%= comment.text %>">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-warning btn-block">Update!</button>
                </div>
            </form>
            <form action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>?_method=DELETE" method="POST">
                <div class="form-group">
                    <button class="btn btn-lg btn-danger btn-block">Delete</button></a>
                </div>
            </form>
            <a href="/campgrounds/<%= campground_id %>"><button class="btn btn-lg btn-default btn-block">Cancel</button></a>
        </div>
    </div>
</div>


<% include ../partials/footer %>